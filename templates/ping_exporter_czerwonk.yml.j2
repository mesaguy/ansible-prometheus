---
#
# Do not edit manually, this file is managed using automation tools
#
{% if prometheus_ping_exporter_czerwonk_cfg is defined and "targets" in prometheus_ping_exporter_czerwonk_cfg %}
  {{ prometheus_ping_exporter_czerwonk_cfg | to_nice_yaml(width=80, indent=2) | indent(2) }}
{% elif prometheus_ping_exporter_czerwonk_cfg is defined %}
targets:
  {{ prometheus_ping_exporter_czerwonk_cfg | to_nice_yaml(width=80, indent=2) | indent(2) }}
{% else %}
targets:
  - localhost
ping:
  interval: 2s
  timeout: 3s
  history-size: 42
  payload-size: 120
{% endif %}
